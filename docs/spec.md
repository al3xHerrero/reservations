# Specifications

> **Note:** This file is a wrapper. The actual source of truth is `/specs.md` at the repository root.

The `/specs.md` file is automatically synced from an external source. To refresh it, run:

```bash
npm run update-specs
```

Do not manually edit `/specs.md` as it will be overwritten on the next sync.
